name: Build DB Migration

on:
  push:
    branches:
      - main
    paths:
      - "devops/helm/db-migration/**"
      - "microservices/db-migration/**"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-db-migration:
    runs-on: ubuntu-latest

    env:
      AWS_REGION: us-east-1
      ECR_REPO: 391701072873.dkr.ecr.us-east-1.amazonaws.com/db-migration

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to ECR
        run: |
          aws ecr get-login-password --region $AWS_REGION \
          | docker login --username AWS --password-stdin 391701072873.dkr.ecr.us-east-1.amazonaws.com

      - name: Build image
        run: |
          docker build -t db-migration microservices/db-migration

      - name: Tag image
        run: |
          docker tag db-migration:latest $ECR_REPO:latest

      - name: Push image
        run: |
          docker push $ECR_REPO:latest