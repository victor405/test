# name: Deploy db-migration

# on:
#   push:
#     branches:
#       - main
#     paths:
#       - "devops/helm/db-migration/**"
#       - "microservices/db-migration/**"
#   workflow_dispatch:

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     permissions:
#       contents: read

#     env:
#       AWS_REGION: us-east-1
#       CLUSTER_NAME: demo-eks
#       RELEASE_NAME: db-migration
#       NAMESPACE: sre-db-migration

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Configure AWS
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Install kubectl
#         run: |
#           curl -LO https://dl.k8s.io/release/v1.29.0/bin/linux/amd64/kubectl
#           chmod +x kubectl
#           sudo mv kubectl /usr/local/bin/

#       - name: Install Helm
#         run: |
#           curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash

#       - name: Configure kubeconfig
#         run: |
#           aws eks update-kubeconfig \
#             --region $AWS_REGION \
#             --name $CLUSTER_NAME

#       - name: Deploy db-migration (Helm)
#         run: |
#           helm upgrade --install $RELEASE_NAME devops/helm/db-migration \
#             --namespace $NAMESPACE \
#             --create-namespace