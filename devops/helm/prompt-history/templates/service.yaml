
apiVersion: v1
kind: Service
metadata:
  name: prompt-history
spec:
  type: {{ .Values.service.type }}
  selector:
    app: prompt-history
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80